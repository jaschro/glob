{
  "name": "glob",
  "private": true,
  "description": "Tests for the browse page and capture form JS -- not part of the built site.",
  "scripts": {
    "test": "node tests/browse.test.js && node tests/add.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
